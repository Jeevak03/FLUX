# GitHub Integration Requirements
requests==2.31.0
gitpython==3.1.40

# Existing requirements (add these if not already present)
fastapi==0.104.1
uvicorn==0.24.0
websockets==12.0
python-dotenv==1.0.0
pydantic==2.5.0
groq==0.5.0