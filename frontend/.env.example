# .env.example - Template for environment variables
# Copy this to .env.local and configure for your environment

# Local Development
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Production (Vercel) - These will be overridden by Vercel settings
# NEXT_PUBLIC_API_URL=https://your-api-domain.com
# NEXT_PUBLIC_WS_URL=wss://your-api-domain.com

# Note: NODE_ENV is automatically set by Next.js
# - 'development' when running 'npm run dev'
# - 'production' when running 'npm run build' or deployed